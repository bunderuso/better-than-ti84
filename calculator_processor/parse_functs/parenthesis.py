def paren_parser(expression, i):
    #defining while loop variables
    start = 0
    start = len(expression) - i
    start = len(expression) - start

    #number storing string
    num_store = ''

    #defining while loop
    #while start < len(expression):
    return 0